# ============================================
# CHC Paint Warehouse Sale — Environment Variables
# Copy this file to .env and fill in your values
# ============================================

# Server
PORT=3000
NODE_ENV=production

# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Warehouse Access Password (legacy fallback — sales now have individual passwords)
WAREHOUSE_PASSWORD=buyfromchc

# JWT Secret for admin authentication (required for admin panel)
JWT_SECRET=change-this-to-a-random-secret-string

# CORS — comma-separated list of allowed origins (optional, defaults to same-origin)
# ALLOWED_ORIGINS=https://your-app.up.railway.app,https://chcsale.com

# Default admin (auto-created on first startup if no admins exist)
# ADMIN_EMAIL=adamberube@me.com
# ADMIN_PASSWORD=CHCadmin2026!
# ADMIN_NAME=Adam Berube

# Email via SMTP (optional — orders still save without email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=CHC Paint <noreply@chcpaint.com>
